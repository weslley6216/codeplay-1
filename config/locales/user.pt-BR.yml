---
pt-BR:
  activerecord:
    models:
      user:
        one: Usuário(a)
        other: Usuários(as)
    attributes:
      user:
        password: Senha
        password_confirmation: Confirmação de Senha
  devise:
    registrations:
      new:
        submit: 'Criar conta'